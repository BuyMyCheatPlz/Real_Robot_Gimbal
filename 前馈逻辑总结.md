# 云台与发射机构前馈逻辑总结

本文汇总 `Real_Robot_Gimbal` 工程中**全部前馈（feedforward）相关逻辑**：标准前馈通路、
被显式关闭/改写的前馈、以及工程里自行新增的补偿与状态量。所有结论都来自当前源码
（`Tasks/Src/pid_calc.c`、`Tasks/Src/pitch_approach.c`、`Tasks/Inc/config.h`、
`Hardware_Drivers/...`），宏取值与代码行为一一对应。

- 控制周期：`CONTROL_PERIOD_S = 1 ms`（`PID_calc` 任务）
- 所有"每拍差量"语义的 D 项：`kd·(e[k]-e[k-1])`，**不除以 dt**
- 方向链：`PITCH_MOTOR_SIGN = +1`、`PITCH_ENCODER_TO_IMU_SIGN = -1`，
  于是 `PITCH_CONTROL_TO_MOTOR_SIGN = PITCH_MOTOR_SIGN * PITCH_ENCODER_TO_IMU_SIGN = -1`
- Yaw 侧：`YAW_MOTOR_COMMAND_SIGN = +1`、`YAW_ENCODER_SIGN = +1`

---

## 1. 前馈在控制链中的位置

```text
                    ┌── 位置 PID ──┐
目标角 ─→ 轨迹规划 ─→│              │─→ 速度目标 ─→ 速度 PID ─┐
          (速度/加速度参考)         │                        ├─→ + ─→ 限幅 ─→ CAN 命令
                    └──────────────┘        前馈 ───────────┘
```

工程里前馈只在**三个注入点**起作用，其余都是"准备前馈量"的辅助逻辑：

| 注入点 | 代码位置 | 注入量 | 单位/量纲 |
|---|---|---|---|
| GM6020 电压前馈（Pitch） | `GM6020_SetVoltageFeedforward()` → `gm6020.c` | 重力前馈 + 静差补偿 +（可选）制动前馈 | GM6020 电压指令（±30000） |
| DM4310 电流前馈（Yaw） | `DM4310_SetCurrentFeedforward()` → `dm4310.c` | 轨迹加速度力矩前馈 | CAN 电流命令单位（±16384） |
| 速度目标前馈（Yaw） | `yaw_speed_target_rad_s = yaw_feedback + yaw_velocity_feedforward` | 轨迹速度前馈 | rad/s（再 × `RAD_S_TO_RPM`） |

> Pitch 侧当前**没有**轨迹速度前馈和加速度前馈（`PITCH_SPEED_LIMIT_ENABLE=0` 时
> 轨迹速度恒为 0，且 Pitch 通路本身不注入轨迹前馈）；诊断与替代手段见 §8.3/§8.5/§11。

---

## 2. Pitch 重力电压前馈（3 阶标定多项式）

### 2.1 计算

`pid_calc.c: pitch_gravity_comp()`

```c
angle_deg = angle_rad * RAD_TO_DEG;
/* 标定区间软钳位：超出范围不外推，按端点值补偿 */
if (angle_deg < PITCH_GRAVITY_FIT_MIN_DEG) angle_deg = PITCH_GRAVITY_FIT_MIN_DEG;
if (angle_deg > PITCH_GRAVITY_FIT_MAX_DEG) angle_deg = PITCH_GRAVITY_FIT_MAX_DEG;

comp = C0 + angle_deg*(C1_gr + angle_deg*(C2 + angle_deg*(C3 + angle_deg*C4)));
```

- 自变量：**连续编码器 Pitch 角** `pitch_angle_actual + pitch_gravity_angle_offset`
  （`pid_calc.c:1153`），不是 IMU 融合角。
- `C1_gr` 是运行时可改变量（`volatile float C1_gr = PITCH_GRAVITY_C1_GR`），
  可用在线命令 `C1_GR=数值` 或 Watch 直接改；`C2/C3/C4/C0` 只能改宏重编。
- 输出直接作为 GM6020 **电压前馈**，与速度 PID 输出相加（§6）。

### 2.2 参数与默认值

| 宏 | 默认值 | 含义 |
|---|---|---|
| `PITCH_GRAVITY_FIT_MIN_DEG` | -50.0 | 标定区间下限，低于此值按 -50° 端点补偿 |
| `PITCH_GRAVITY_FIT_MAX_DEG` | +26.0 | 标定区间上限 |
| `PITCH_GRAVITY_POLY_C0` | 1678.105241 | 常数项 |
| `PITCH_GRAVITY_C1_GR` | 161.466898 | 一次项（上电初值，运行时可调） |
| `PITCH_GRAVITY_POLY_C2` | -0.590091 | 二次项 |
| `PITCH_GRAVITY_POLY_C3` | 0.006451 | 三次项 |
| `PITCH_GRAVITY_POLY_C4` | 0.0 | 四次项（当前关闭） |

### 2.3 为什么用编码器角而不是 IMU 角

README/config 的注释给出了设计理由：IMU 融合角与其低通在阶跃/快速运动时滞后，
滞后角进入重力前馈会变成"延迟的补偿 → 推动超调"。连续编码器角没有这个滞后。
旧宏 `PITCH_GRAVITY_ROLL_LPF_ALPHA = 0.05` 是上一代 IMU Roll 前馈的滤波系数，
现在不再进入前馈路径（保留仅为历史）。

### 2.4 单独标定模式

`PITCH_GRAVITY_ONLY_ENABLE = 1` 时（`pid_calc.c:1052`）：

- 位置 PID 归零复位、`PitchApproach_Reset()`、速度环增益强制 `0/0/0`；
- `pitch_speed_target_rpm = 0`，轨迹速度/加速度清零；
- 仍然输出重力前馈，`pitch_motor_feedforward = gravity_feedforward`（不含静差/制动补偿）。

此时 VOFA Pitch 调参页的 **I6（电机坐标系总前馈）/ I7（最终 CAN 电压命令）**
就是纯重力前馈输出，用于标定曲线（`vofa.c:210-221`）。标定完成后必须改回 0，
因为只有前馈无法全角度可靠保持。

---

## 3. Yaw 轨迹速度前馈

`pid_calc.c:950-956`

```c
yaw_trajectory_step(&yaw_profile_target, &yaw_profile_speed, &yaw_profile_acceleration,
                    yaw_target, YAW_TRAJECTORY_MAX_SPEED_RAD_S,
                    YAW_TRAJECTORY_MAX_ACCEL_RAD_S2, control_dt_s);
yaw_velocity_feedforward = YAW_VELOCITY_FF_GAIN * yaw_profile_speed;
```

- 参考量来自**限速度/限加速度轨迹**（不是对位置阶跃求导），因此前馈本身连续。
- 注入方式是**加在速度目标上**：

```c
yaw_speed_target_rad_s = clampf(yaw_feedback + yaw_velocity_feedforward, YAW_MAX_SPEED_RAD_S);
DM4310_SetSpeed(&can2_dm4310_id1,
    YAW_MOTOR_COMMAND_SIGN * yaw_speed_target_rad_s * RAD_S_TO_RPM * YAW_ENCODER_TO_OUTPUT_RATIO);
```

- 作用：位置环不需要靠 P 产生"跟随速度所需的稳定速度分量"，降低跟踪误差与相位滞后。
- 系数含义：`1.0` = 完整使用规划速度；当前 `0.89`（略保守，用于压低进目标时的尾速）。
- 进入 Yaw 保持（hold）状态时该前馈被强制清零（§7.3）。

| 宏 | 默认值 | 含义 |
|---|---|---|
| `YAW_VELOCITY_FF_GAIN` | 0.89 | 轨迹速度前馈增益（1.0=全量） |
| `YAW_TRAJECTORY_MAX_SPEED_RAD_S` | 8.8 | 规划最大速度（输出轴 rad/s） |
| `YAW_TRAJECTORY_MAX_ACCEL_RAD_S2` | 87.0 | 规划最大加速度 |
| `YAW_MAX_SPEED_RAD_S` | 7.0 | 叠加前馈后速度目标的最终限幅 |

---

## 4. Yaw 轨迹加速度力矩前馈

`pid_calc.c:957-960`

```c
yaw_acceleration_feedforward = clampf(
    YAW_ACCELERATION_FF_CURRENT_PER_RAD_S2 * yaw_profile_acceleration,
    YAW_ACCELERATION_FF_CURRENT_LIMIT);

DM4310_SetCurrentFeedforward(&can2_dm4310_id1,
    YAW_MOTOR_COMMAND_SIGN * yaw_acceleration_feedforward);
```

- 量纲刻意做成"**CAN 电流命令单位 / (输出轴 rad/s²)**"，可直接读数值现场调参：
  给多少电流命令单位来产生 1 rad/s² 的输出轴角加速度，本质是惯量前馈 `J`。
- 正值表示输出轴正加速度；符号由 `YAW_MOTOR_COMMAND_SIGN` 统一施加。
- 限幅 `YAW_ACCELERATION_FF_CURRENT_LIMIT = 16384`（DM4310 协议上限）。

| 宏 | 默认值 | 含义 |
|---|---|---|
| `YAW_ACCELERATION_FF_CURRENT_PER_RAD_S2` | 15.0 | 加速度前馈系数 |
| `YAW_ACCELERATION_FF_CURRENT_LIMIT` | 16384.0 | 加速度前馈限幅 |

### 现场整定顺序（README 记录的经验）

1. 先把 `YAW_ACCELERATION_FF_CURRENT_PER_RAD_S2` 保持 `0`，固定轨迹最大速度/加速度；
2. 把 `YAW_VELOCITY_FF_GAIN` 从 0 以 0.2 递增，直到跟踪误差不再明显减小；
3. 再把加速度前馈系数从小到大加。
4. 判据：若 VOFA Yaw 页 I4（速度目标）仍朝向目标，而 I7（最终电流命令）已提前反向，
   说明**加速度前馈过强**，先减小该系数或增大速度环 P。

---

## 5. Pitch 动态制动前馈（已实现，当前关闭）

`Tasks/Src/pitch_approach.c: PitchApproach_UpdateBrakeFeedforward()`
（`PITCH_APPROACH_BRAKE_FF_ENABLE = 0U`，代码保留、不参与控制）

设计逻辑（若开启）：

1. 按运动方向与目标角选择**停车模型** `pitch_brake_profile()`：

   | 工况 | 条件 | 等效减速度 | 响应延迟 | 增益 | 限幅 |
   |---|---|---|---|---|---|
   | 普通下降 | 实际速度 < 0 | 2500 °/s² | 30 ms | 120 | 8000 |
   | 低目标下降 | 速度<0 且目标 ≤ -45° | 2500 °/s² | 30 ms | 120 | 8000 |
   | 低角度向上 | 速度>0 且目标 ≤ -25° | 2500 × 0.60 = 1500 °/s² | 30 ms | 180 | 8000 |
   | 高角度向上 | 速度>0 且目标 > -25° | 2500 × 2.00 = 5000 °/s² | 55 ms | 90 | 4500 |

   低角度向上在误差 6°→4° 之间用 smoothstep 渐入（`profile.fade`）。

2. 由"含响应延迟的停车距离"反解允许速度：

   ```text
   v_stop = sqrt( (a·t_delay)² + 2·a·|e| ) - a·t_delay
   ```

3. 超速量 `overspeed = |v_actual| - max(v_stop, PITCH_APPROACH_BRAKE_FF_STOP_SPEED_DEG_S)`；
4. `target_voltage = overspeed × gain × fade`，限幅后乘 `-control_to_motor_sign × motion_direction`
   （保证制动力与物理速度反向，再折算到电机坐标）；
5. 过零保护：目标电压与当前电压异号时先把状态清零，避免反向冲击；
6. 变化率限制 `PITCH_APPROACH_BRAKE_FF_SLEW_VOLT_PER_S = 400000 V/s`
   （= 每 1 ms 拍最多变化 400 个电压单位），返回状态量 `state->brake_voltage`。

**为什么关闭**：该前馈与速度 PI 会对同一个"超速量"重复制动，实测造成到位前反转
（`config.h:214-217`、`Tasks/README.md:35`）。当前制动完全交给速度 PI 与
`PitchApproach_LimitSpeed()` 的**速度目标限幅**（限速只收缩、不反向，见 §8.3）。

---

## 6. 电机驱动层的前馈处理（前馈真正生效的地方）

### 6.1 GM6020：前馈占用余量，PID 拿到非对称边界

`Hardware_Drivers/Gm6020/Src/gm6020.c:81-99`

```c
/* 前馈会占用一部分执行器余量。把剩余的非对称上下限传给 PID，
 * 使积分器看到真正的最终饱和边界，而不是在 PID+前馈被二次裁剪时饱和累积。 */
feedback_output = MotorSpeedPid_CalculateFloatBounded(
    &motor->speed_pid, target, filtered_speed, dt_s,
    -GM6020_VOLTAGE_LIMIT - motor->voltage_feedforward,   /* 下界 */
     GM6020_VOLTAGE_LIMIT - motor->voltage_feedforward);  /* 上界 */
output = feedback_output + motor->voltage_feedforward;
/* 最终再做一次硬钳位 */
```

要点：

- 前馈先被 `GM6020_SetVoltageFeedforward()` 钳到 `±GM6020_VOLTAGE_LIMIT(30000)`；
- PID 的上下限变成**非对称**的 `[-(L-ff), +(L-ff)]`，抗积分饱和条件按真实饱和边界判断
  （`motor_common.c:97-99`：出界且误差仍指向内侧就不吃积分）；
- `last_feedback_output` 只记录**不含前馈**的 PID 输出，方便整定时区分"PID 在推"还是
  "前馈在顶"；`last_output` 记录含前馈的最终值。

### 6.2 DM4310：电流前馈 + 误差扩散量化

`Hardware_Drivers/Dm4310/Src/dm4310.c:109-136`

```c
output = MotorSpeedPid_CalculateFloat(...) + motor->current_feedforward;
output 限幅到 ±min(speed_pid.output_limit, DM4310_CURRENT_COMMAND_LIMIT);

/* 误差扩散：把不足 1 个计数的 PID/前馈输出跨帧保留 */
quantized_input = output + motor->current_quantization_error;
command = 四舍五入(quantized_input);
motor->current_quantization_error = quantized_input - command;
```

- 前馈与 PID 输出**先相加再统一限幅**（与 GM6020 的"预留下界"策略不同）；
- 启动最小电流补偿：`|目标转速| ≥ YAW_STARTUP_SPEED_THRESHOLD_RPM(0.20 rpm)`
  且 `|output| < YAW_STARTUP_MIN_CURRENT(80)` 时，把输出顶到 ±80，
  用于克服静摩擦（这属于"最小力矩前馈"性质，但走的是驱动层）；
- 误差扩散让"前馈 + PI"的**小数部分不会因整数截断而死区**，
  例如长期请求 0.25 会间歇发出 1，长期均值为 0.25，同时单帧不越限。

### 6.3 发射机构

`launch.c` 中 M3508 / M2006 的闭环**没有前馈项**，只做速度环（M2006 另有角度环）。
`PITCH_STARTUP_MIN_VOLTAGE = 0`（Pitch 的"起步助推"继电器已关闭，
它曾把保持段变成 3.3~3.6 Hz 极限环）。

---

## 7. 前馈的使能、冻结与清零（安全门控）

前馈量不是无条件输出的，以下路径会把它置零或冻结：

### 7.1 权限级清零

`pitch_control_permitted == 0` 或 `yaw_control_permitted == 0` 时
（IMU 不新鲜、CAN 发送故障、控制超期、遥控不新鲜、对应电机离线、
`YAW_CLOSED_LOOP_ENABLE=0` 等），复位对应 PID 与 `PitchApproach_Reset()`。

### 7.2 停机 / 辨识 / 故障分支显式写零

| 分支 | 代码 | 处理 |
|---|---|---|
| Yaw 系统辨识固件 `YAW_SYSID_MODE=1` | `pid_calc.c:879,887` | `GM6020_SetVoltageFeedforward(0)`、`DM4310_SetCurrentFeedforward(0)` |
| DM4310 离线 / 无目标（停机） | `pid_calc.c:916-919, 1217-1220` | 两轴前馈都写 0 |
| 电机更新失败 | `pid_calc.c:1196-1212` | 复位目标与状态，`gravity_feedforward = 0` |
| Pitch 大阶跃目标重锚 | `pid_calc.c:626-632` | `PitchApproach_Reset()` 清 `brake_voltage / static_voltage`，轨迹速度/加速度清零 |

### 7.3 Yaw 保持（hold）期间前馈全清

`yaw_hold_state.active` 进入时（`pid_calc.c:969-982`）：位置 PID 与速度 PID 复位一次，
`yaw_feedback / yaw_velocity_feedforward / yaw_acceleration_feedforward` **全部置 0**。
原因：到位保持阶段不应再有轨迹前馈推力，否则形成"刹车→重启"的不连续极限环
（代码注释：每毫秒重复复位会制造该极限环，因此只在状态切换时复位一次）。

### 7.4 Pitch 锁存（yaw 运动期间）时冻结前馈

`pid_calc.c:1022-1051`：收到 yaw 遥控指令后 500 ms 窗口内，Pitch 进入 `output_hold`，
`GM6020_Update()` 直接返回 `held_output`，PID/滤波状态冻结；
此时重力前馈沿用进入锁存时保存的 `pitch_held_gravity_ff`，
即**前馈值冻结在上一时刻，不再随角度更新**。解锁时复位速度环与滤波初值，
避免保持期与恢复期状态衔接跳变。

---

## 8. 工程自行新增的逻辑（非教科书标准前馈部分）

以下是本工程在前馈/补偿链上额外的设计，均可在代码中原位对应：

### 8.1 重力前馈采用"编码器角 + 三段区间"而非 IMU 角

- 用连续编码器角做自变量，规避 IMU 融合角与低通滞后（§2.3）；
- 标定区间外**端点保持**而非多项式外推，避免区间外补偿发散。

### 8.2 `C1_gr` 提为运行时可调量

`volatile float C1_gr`，支持 `C1_GR=数值` / `PITCH_GRAVITY_C1_GR=数值` 在线命令，
无需重编译即可现场微调线性项（重力方向验证、负载变化时最常用的一项）。

### 8.3 小误差静差补偿：带变化率限制的**状态量**

`pitch_approach.c: PitchApproach_UpdateStaticErrorComp()`，
`PITCH_STATIC_ERROR_COMP_ENABLE = 1`（**当前启用**，与制动前馈不同）。

输出叠加在 Pitch 总前馈里（`pid_calc.c:1156-1164`）：

- 生效条件：`|误差| > 0.08°` 且 `|实际速度| < 8 °/s`；
- 有效误差在 `1.0°` 处封顶（超过后保持端点值，不再在边界突然撤掉）；
- 速度渐隐：8 °/s 以下按比例线性衰减到 1 °/s 处全量；
- 分区增益：目标角 ≤ -45° 时用 `5000 V/° / 限幅 1000`（低刚度），
  其余角度用 `20000 V/° / 限幅 3200`；
  原因是 -60° 区域的瞬时静差补偿会与机构 17.5 Hz 模态形成极限环；
- **变化率限制**：`PITCH_STATIC_ERROR_COMP_SLEW_VOLT_PER_S = 40000 V/s`
  （每 1 ms 最多变 40 个电压单位），把"瞬时补偿"变成连续状态量；
- 方向：`× control_to_motor_sign × sign(error)`，即补偿方向永远指向减小误差。

### 8.4 动态制动前馈"写了但关"的保留开关

`PITCH_APPROACH_BRAKE_FF_ENABLE` 与四组工况增益/限幅宏全部保留，
配套的 `PitchBrakeProfile_t`/`pitch_stop_speed()` 被**限速路径复用**（§8.5），
因此这套停车模型即便前馈关闭也不是死代码。

### 8.5 停车距离**速度目标限幅**（替代制动前馈的制动手段）

`PitchApproach_LimitSpeed()`，`PITCH_APPROACH_SPEED_LIMIT_ENABLE = 1`：

```text
v_allowed = max( sqrt((a·t_delay)² + 2·a·|e|) - a·t_delay , v_min )
v_target  = sign(error) · min(朝向目标的速度分量, v_allowed)
```

- 最小速度 `PITCH_APPROACH_MIN_SPEED_DEG_S = 25 °/s`，并在 0.6° 误差内渐隐到 3 °/s；
- 只允许"朝向目标或为零"，**不生成反向速度目标**（这是与制动前馈的关键区别，
  也是它不会和速度 PI 重复制动的原因）；
- 三套停车模型与 §5 表格一致（含响应延迟 30/55 ms）。

### 8.6 `pitch_feedback_output` 与 `pitch_motor_feedforward` 双通道遥测

`GimbalControlState_t` 同时暴露：

- `pitch_feedback_output` = 不含前馈的 PID 输出（`last_feedback_output`）；
- `pitch_motor_feedforward` = 电机坐标系总前馈（`voltage_feedforward`）；
- `pitch_can_command` = 最终 CAN 电压命令。

Pitch 调参页 I5/I6/I7 即这三者，可一眼判断"是 PID 饱和还是前馈顶住"。

### 8.7 前馈输出在锁存/停机/故障下的一致处理

前馈的"冻结-清零-恢复"全链路（§7）是本工程为 yaw→pitch 通道串扰问题专门加的：
`GM6020` 增加 `output_hold / held_output / last_output` 三个字段，
把"锁存期间前馈与 PID 都不更新"做成驱动层能力。

---

## 9. 全量前馈开关与参数总表

| 前馈/补偿 | 开关宏 | 当前值 | 参数 | 注入点 |
|---|---|---|---|---|
| Pitch 重力电压前馈 | 无（常开） | 开 | `PITCH_GRAVITY_POLY_C0/C2/C3/C4`、`C1_gr`、`FIT_MIN/MAX_DEG` | GM6020 电压前馈 |
| Pitch 静差补偿 | `PITCH_STATIC_ERROR_COMP_ENABLE` | **1** | `*_DEADBAND/MAX_ERROR/FADE_SPEED/FULL_SPEED/VOLT_PER_DEG/LIMIT/LOW_ANGLE_*/SLEW_VOLT_PER_S` | 同上（相加） |
| Pitch 动态制动前馈 | `PITCH_APPROACH_BRAKE_FF_ENABLE` | **0** | `DOWN_/LOW_ANGLE_UP_/HIGH_ANGLE_UP_BRAKE_FF_GAIN/LIMIT`、`BRAKE_FF_STOP_SPEED`、`SLEW` | 同上（相加，当前恒 0） |
| Pitch 仅重力标定模式 | `PITCH_GRAVITY_ONLY_ENABLE` | 0 | — | 旁路位置/速度环 |
| Yaw 轨迹速度前馈 | 无（常开） | 开 | `YAW_VELOCITY_FF_GAIN`、`YAW_TRAJECTORY_MAX_SPEED/ACCEL_*` | 速度目标相加 |
| Yaw 加速度力矩前馈 | 无（常开） | 开 | `YAW_ACCELERATION_FF_CURRENT_PER_RAD_S2 / _LIMIT` | DM4310 电流前馈 |
| DM4310 小数误差扩散 | 无（常开） | 开 | — | 量化前累积误差 |
| Yaw 启动最小电流 | 无（常开） | 开 | `YAW_STARTUP_SPEED_THRESHOLD_RPM / MIN_CURRENT` | 驱动层最小力矩 |
| Pitch 起步助推 | 由 `PITCH_STARTUP_MIN_VOLTAGE` | **0（关）** | `PITCH_STARTUP_SPEED_THRESHOLD_RPM` | 驱动层（已关闭） |

### 相关限幅/单位

| 量 | 值 | 说明 |
|---|---|---|
| `GM6020_VOLTAGE_LIMIT` | 30000 | GM6020 电压指令上限，前馈单独也受此限 |
| `DM4310_CURRENT_COMMAND_LIMIT` | 16384 | DM4310 电流命令上限 |
| `PITCH_SPEED_OUTPUT_LIMIT` | 30000 | Pitch 速度环输出限幅（PID 部分） |
| `YAW_CURRENT_OUTPUT_LIMIT` | 16384 | Yaw 速度环输出限幅 |
| `PITCH_APPROACH_BRAKE_ACCEL_DEG_S2` | 2500 °/s² | 停车模型基准减速度 |
| `CONTROL_PERIOD_S` | 1 ms | 所有 slew 限制的换算基准 |

---

## 10. 观测与调参入口（VOFA）

| 页面 | 宏 | 与前馈相关的通道 |
|---|---|---|
| Pitch 调参页 | `VOFA_PITCH_TUNING_MODE=1` | I5=速度 PID 输出（不含前馈）、**I6=电机坐标系总前馈**、**I7=最终 CAN 电压命令** |
| Yaw 调参页 | `VOFA_YAW_TUNING_MODE=1` | I4=速度目标（含速度前馈）、I6=轨迹速度（前馈源）、I7=最终电流命令（含加速度前馈） |
| 默认综合页 | 全部为 0 | I1/I2 Pitch 目标/实际角（重力前馈标定用） |

在线命令（UART4，ASCII）：

```text
C1_GR=161.5            # 或 PITCH_GRAVITY_C1_GR=161.5  → 改重力前馈一次项
PITCH_KP_POS=... PITCH_KI_POS=... PITCH_KD_POS=...
PITCH_KP_SPD=... PITCH_KI_SPD=... PITCH_KD_SPD=...
YAW_KP_POS=... YAW_KI_POS=... YAW_KD_POS=...
YAW_KP_SPD=... YAW_KI_SPD=... YAW_KD_SPD=...
```

注意：在线命令只改运行时变量/PID 与 `C1_gr`，**不写 Flash**，
复位后恢复 `config.h` 编译初值；`YAW_VELOCITY_FF_GAIN` 与
`YAW_ACCELERATION_FF_CURRENT_PER_RAD_S2` 目前只能改宏重编。

---

## 11. 风险点与操作注意事项

1. **重力前馈方向/曲线错误会持续推动 Pitch**。首次测试建议把 `C1_gr` 调小，
   用 `PITCH_GRAVITY_ONLY_ENABLE=1` 单独看 I6/I7 方向，确认后再并回闭环。
2. **静差补偿是当前唯一使能的自定义前馈项**，它与机构 -60° 附近的 17.5 Hz 模态
   耦合过；若该处出现持续抖动，先确认低角度分区参数与 slew 限幅是否被改动。
3. **制动前馈与速度 PI 会争抢同一超速量**，重新打开前必须把速度环积分分离/增益
   一并重调，否则会重现"到位前反转"。
4. **Yaw 加速度前馈过大**表现为 VOFA I7 比 I4 提前反向，先减该系数再动速度环 P。
5. **Pitch 侧没有轨迹速度/加速度前馈是刻意设计**（`PITCH_SPEED_LIMIT_ENABLE=0` 时
   轨迹速度恒为 0），不要误以为漏实现；若把该开关置 1，则轨迹前馈量会被计算出
   但仍不注入（`pid_calc.c:1155` 起只注入重力+静差+制动三项）。
